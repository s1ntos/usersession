spring.application.name=session
# Conexão com MySQL

spring.datasource.url=jdbc:mysql://localhost:3306/usersession
spring.datasource.username=root
spring.datasource.password=joaocosta34

# Configurações JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Fuso horário (evita warning)
spring.datasource.hikari.connection-timezone=UTC